export const dropDownValues = () => {
    const restSearchPlaceholder = {
        name: "Search by name..",
        zip: "Search by zip.."
    }

    return {restSearchPlaceholder};
}

export default dropDownValues;